// Copyright 2019 Jakob Sailer
// Author: Jakob Sailer <sailer.jakob@web.de>

#include <assert.h>

#include "./board.h"
#include "./frame.h"

int main(void) {
  /*
  * I dont think its neccessary to test here
  * All functions pretty much just do prints
  *
  struct board b;
  char *filename = "save1.4";
  FILE *f = fopen(filename, "r");
  init_board(&b, f);
  fclose(f);
  assert(print_frame(&b));
  for (int i = 0; i < 5; i++) {
    assert(print_error(i));
  }
  assert(print_help());
  */
  return 0;
}
